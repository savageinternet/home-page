<!DOCTYPE html>
<html>
{{> head}}
<body>
<div id="root">
  {{> header}}
  <div id="body">
    <img class="project-image" src="/img/projects/{{thumb}}" alt="{{title}}">
    <div class="project-title">{{project.title}}</div>
    <div class="project-links">
      {{#project.links}}
        <a href="{{href}}">{{title}}</a>
      {{/project.links}}
    </div>
    <div class="project-desc">
        {{project.description}}
    </div>
    This should display whole tag names... :(
    <div class="project-tags">
      {{#project.tags}}
        <div class="tag {{.}}">{{.}}</div>
      {{/project.tags}}
    </div>
</div>
</body>
</html>
